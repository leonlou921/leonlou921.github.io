# Zhenye Lou Personal Homepage

Static GitHub Pages homepage for `zhenyelou.github.io` or any repository served from the repository root.

## Local Preview

Open `index.html` directly in a browser, or serve the folder with any static HTTP server.

## GitHub Pages

Use GitHub Pages with the source set to the root of the default branch. The site has no build step and no external runtime dependencies.
